/**
 * Canonical community endpoints.
 *
 * The invite lives here rather than inline in each surface: the footer
 * previously pointed at `discord.gg/Kaiwu`, which is not a real invite
 * (Discord answers it with `10006 Unknown Invite`), and nothing caught it
 * because no other surface shared the value.
 */

/** Invite for the "Kaiwu devs" server (guild 1467127365317558402). */
export const DISCORD_INVITE_URL = 'https://discord.gg/W6Pb8KuHfg';
