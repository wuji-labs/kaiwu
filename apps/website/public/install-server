#!/usr/bin/env bash
set -euo pipefail

# Convenience wrapper for server installs.
# Uses the shared installer with KAIWU_PRODUCT=server.
export KAIWU_PRODUCT="${KAIWU_PRODUCT:-server}"

curl -fsSL "https://kaiwu.chengqiyun.com/install.sh" | bash
