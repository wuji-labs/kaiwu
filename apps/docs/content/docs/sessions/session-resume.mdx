---
title: Resume inactive sessions
description: Resume an inactive Kaiwu session from the app, web UI, or CLI without starting over.
---

Session resume lets you continue an **inactive Kaiwu session** by asking the provider backend to reopen the same provider session, then reconnecting that provider session to the same Kaiwu session.

<Callout type="info">
  Sending to a resumable inactive session while its machine is offline is currently available in development builds and is not yet a released contract.
</Callout>

This is different from:

- **Attach**: reconnecting to a session that is still running locally through its live terminal/provider host
- **Browse Direct sessions**: opening a provider-native session that Kaiwu does not own yet
- **Resume session**: starting a new Kaiwu session by resuming a session the agent already has (see [Continuing a session](/sessions/continuing-a-session))
- **Starting a new session**: creating a fresh provider session with no vendor resume

## What “resume” means in Kaiwu

In Kaiwu, resume is a strict workflow:

1. Kaiwu finds the existing Kaiwu session
2. Kaiwu checks whether that session is actually **vendor-resumable**
3. Kaiwu asks the backend/provider to resume the same vendor session
4. If that vendor resume fails, Kaiwu shows an error instead of silently starting a new session

That strict behavior matters because it keeps one session identity and one transcript source of truth.

When a synced Kaiwu session resumes, Kaiwu continues the existing session. It does **not** create a second imported-history pass just because you resumed it.

## When a session is resumable

A session is resumable only when all of these are true:

- the session is a normal Kaiwu-managed session
- it is currently **inactive**
- it is **not archived**
- Kaiwu has a valid provider resume id stored in session metadata
- the selected backend/provider supports resume for that session
- your current settings do not disable that backend

If any of those checks fail, Kaiwu treats the session as **not resumable** and shows that clearly in the UI or CLI.

## App and web behavior

In the app or web UI, an inactive session can still accept input **only if it is resumable**.

Typical behavior:

- **Inactive + resumable + machine reachable**: you can send a message; Kaiwu either attempts to resume it or leaves it queued according to your automatic-resume policy
- **Inactive + resumable + machine offline**: you can still send; Kaiwu keeps the message in the Pending queue and starts it after reconnect only if that send has been authorized for processing when available
- **Inactive + not resumable**: input is disabled

The session continues to show **offline** while its machine is unavailable. A banner above the composer explains whether a queued message is waiting for automatic resume, has been left queued by your preference, or needs an explicit retry after a start failure.

Under **Settings → Session → Message sending**, **Automatic resume after sending** controls future sends to inactive sessions. The default, **Only if machine is online**, makes one immediate attempt when the machine is reachable but never parks authorization for a later restart. You can instead choose **Now or when machine returns** or **Never automatically**. Changing the setting does not cancel a resume already requested by an earlier send.

## CLI behavior

Kaiwu CLI exposes two main workflows for resume.

### List sessions

```bash
happier session list
```

By default this prints a table with:

- shortened session id
- agent
- last updated time
- active state
- resume status
- title
- working directory path

Useful variants:

```bash
happier session list --resumable
happier session list --plain
happier session list --json
```

Use:

- `--resumable` to show only sessions that can currently be resumed
- `--plain` for the older one-line output format
- `--json` for scripting

### Resume interactively

```bash
happier resume
```

In a real TTY, Kaiwu opens an interactive selector and shows resumable inactive sessions sorted by recent activity.

If your terminal does not support the interactive selector, Kaiwu tells you to use `happier session list --resumable` and then resume by id.

### Resume by id

```bash
happier resume <session-id>
```

You can pass a full session id or an unambiguous prefix.

Kaiwu then:

1. resolves the session
2. validates that it is inactive, not archived, and resumable
3. restores the session’s working directory
4. resumes the provider session
5. reconnects that provider session to the same Kaiwu session

If the session is active already, archived, missing its working directory, or not vendor-resumable, the command fails with a clear error.

## Resume status in the CLI table

The `RESUME` column is a quick summary:

- `yes`: resumable
- `no(unsupported)`: that agent/backend does not support vendor resume
- `no(missing-id)`: the session does not have the provider resume id Kaiwu needs
- `no(disabled)`: resume exists for that backend, but your current settings disable it
- `no(off)`: account settings disabled that backend entirely

## Codex-specific behavior

Codex resume in Kaiwu normally uses the default App Server path. ACP resume is still available when you explicitly choose ACP.

Important points:

- Codex now defaults to **App Server** in Kaiwu
- legacy `mcp_resume` behavior is gone
- if you opt Codex back to plain **MCP**, fresh sessions can still start there, but vendor resume is not available on that path
- for new Codex sessions, Kaiwu does not silently fall back from the default App Server path; choose ACP or MCP explicitly if you want a different backend
- App Server is the default resumable path; ACP still works when you explicitly select it
- for an explicit **resume**, Kaiwu stays strict and errors instead of falling back

So for Codex, “can I resume this session?” depends on the session metadata and on whether the current backend path supports resume for that session.

## Resume vs attach

Use **resume** when the session is inactive and the provider supports reopening that provider session.

Use **attach** when the session is still running locally and you want to reconnect to the live process:

```bash
happier attach <session-id>
```

If you are unsure:

- use `attach` for a still-running local session
- use `resume` for an inactive session you want to continue

Attach can mean different things depending on how the session was started:

- tmux reattach,
- focusing an existing Windows Terminal or Console host,
- or using a provider-native attach flow when the provider supports it.

## Resume vs Direct sessions

Direct sessions are provider-native sessions that Kaiwu can inspect or follow, but that Kaiwu does not fully own yet.

For those sessions:

- browsing/opening happens through the **Direct** sessions flow
- syncing/importing happens through **Take over + import**
- normal Kaiwu resume applies after the session is a synced Kaiwu session and has valid vendor-resume metadata

## Things to check if resume is unavailable

If a session does not resume, check:

- is the session inactive rather than already running?
- is it archived?
- does it belong to the provider/backend you think it does?
- are you on the correct Kaiwu server/account profile?
- for Codex, is the selected resume path available (App Server by default, or ACP when explicitly selected)?
- is the owning machine online and reachable from Kaiwu?

## Related

- [Attach to a running session](/sessions/attach-to-session)
- [Continuing a session](/sessions/continuing-a-session)
- [CLI](/apps/cli)
- [Codex](/agents/codex)
- [Permissions](/sessions/permissions)
