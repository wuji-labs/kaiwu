---
title: OpenCode
description: Configure OpenCode in Kaiwu, including the default server-native backend, app ↔ terminal control switching, and attach behavior.
---

This page documents how Kaiwu integrates with **OpenCode** and what matters for day-to-day use.

## Requirements

- OpenCode CLI installed on your machine.
- Binary available on `PATH` (or set `HAPPIER_OPENCODE_PATH`).

## Install OpenCode

If you use Kaiwu's provider installer, you can install OpenCode from the CLI:

```bash
happier install provider opencode
opencode --version
```

Or install manually:

```bash
curl -fsSL https://opencode.ai/install | bash
opencode --version
```

On Windows, OpenCode also supports native installs such as:

```bash
npm install -g opencode-ai
opencode --version
```

If Kaiwu offers to run OpenCode’s vendor install recipe for you, it requires explicit confirmation before executing vendor install commands.

## Local authentication in Kaiwu

OpenCode supports machine-local auth management in:

- **Settings → AI & Agents → AI backends → OpenCode**

From there you can:

- check whether OpenCode is authenticated on the selected machine
- open `opencode auth login` directly inside Kaiwu
- refresh the auth state with **Check now**

Kaiwu reads OpenCode’s local auth state through the CLI’s own auth listing flow when available.

For the shared user guide, see [Provider authentication](/agents/provider-authentication).

## How Kaiwu runs OpenCode

OpenCode has two runtime paths in Kaiwu:

- **Server-native (default)**: Kaiwu talks to the OpenCode server/session APIs directly. This is the normal path for new OpenCode sessions in Kaiwu.
- **ACP (legacy fallback)**: still available when you explicitly switch OpenCode to ACP mode.

For most users, the important part is:

- new OpenCode sessions default to the **server-native** backend,
- Kaiwu can keep the same OpenCode session visible in both the app and the terminal,
- attaching later should use `happier attach <session-id>`, not a raw vendor attach command.

By default, Kaiwu uses a **managed local OpenCode server** on the machine and reuses it across sessions. Advanced users can set an explicit OpenCode server URL in settings; when you do that, Kaiwu will keep using that explicit server for the session.

## Start OpenCode from a terminal

```bash
happier opencode
```

When you start OpenCode this way:

- the session appears in Kaiwu as a normal session,
- the terminal starts in **local/shared control**,
- the Kaiwu app can still see the same transcript and send messages into the same OpenCode session,
- messages typed directly into the OpenCode TUI also show up in Kaiwu.

This is useful when you want the provider TUI first, but still want the app to stay attached to the same session.

### Detach from the app

If the session is locally attached, the session header in Kaiwu shows **Detach terminal**.

Using it:

- switches the session back to **remote** control,
- keeps the same session id and transcript,
- leaves the existing terminal open in a read-only/logs-only state until you attach again.

You can later run `happier attach <session-id>` from a terminal on the same machine to get back into the OpenCode TUI.

## Start OpenCode from the app

If you start a new OpenCode session from the Kaiwu app:

- Kaiwu starts it in **remote/server-only** mode in the background,
- the app can use the session immediately,
- no terminal needs to be open yet.

Later, on the same machine, you can attach a terminal to that live session with:

```bash
happier attach <session-id>
```

After attaching:

- the session switches into **local/shared control**,
- the terminal shows the OpenCode TUI,
- the app still stays connected to the same session and transcript.

This is the main “start in the app, continue in the terminal” workflow for OpenCode.

## Local vs remote control in OpenCode

OpenCode uses **shared local control** in Kaiwu.

That means:

- **Remote mode**: Kaiwu drives OpenCode in the background and you use the app/web UI
- **Local mode**: you use the OpenCode TUI in a terminal
- the transcript stays shared in both modes
- the session stays writable from the app even when a terminal is attached
- app messages do not need the same exclusive pending-queue handoff used by Claude and Codex local control

In practice, you can:

- send a message from the app and watch it appear in the attached OpenCode TUI,
- send a message from the OpenCode TUI and see it appear in Kaiwu,
- detach and reattach without starting a new session.

For the full switching workflow, see [Features → Attach to a running session](/sessions/attach-to-session).

## Authentication (connected services)

OpenCode can reuse connected services profiles when spawned by Kaiwu (for example):

- `openai-codex` (subscription OAuth) or `openai` (API key) for OpenAI-backed usage
- `claude-subscription` setup-token profiles or `anthropic` (API key) for Anthropic-backed usage

If multiple compatible profiles exist, Kaiwu will let you pick one when starting a new session.

OpenCode accepts `claude-subscription` only for setup-token profiles. Claude subscription OAuth profiles are shown as setup-token action rows and cannot be selected as an OpenCode session binding.

See: [Connected services](/accounts/connected-services)

## Session handoff

OpenCode supports Kaiwu session handoff between machines.

This is a natural fit for OpenCode because Kaiwu can move the same Kaiwu session while preserving OpenCode continuity on the new machine.

Use it when you want to:

- move a live OpenCode session from one machine to another
- keep the same Kaiwu session id
- continue the same work from the target machine

Connected services can help here too, especially when the target machine should reuse account-managed credentials instead of depending only on a local machine login state.

See:

- [Session handoff](/sessions/session-handoff)
- [Connected services](/accounts/connected-services)

For the full user-facing MCP guide in Kaiwu, see:

- [MCP servers](/extending/mcp-servers)

## Permissions, modes, and models

Kaiwu uses the same provider-agnostic permission picker for OpenCode as it does for other agents.

Important OpenCode notes:

- permission intent is chosen in Kaiwu,
- Kaiwu maps that intent to the closest OpenCode session permission ruleset,
- some OpenCode behavior still depends on the installed OpenCode runtime.

OpenCode can also expose provider-native session modes and model choices. When those are available, Kaiwu surfaces them in the session UI.

For the full user guide, see:

- [Model and engine selection](/agents/model-and-engine-selection)
- [Permissions](/sessions/permissions)

## Notes

- Resume availability is session-specific in Kaiwu: the session must have valid OpenCode resume metadata, and your current backend/settings must allow resume.
- Attach availability is also session-specific: the session must have the metadata/context Kaiwu needs to reopen that OpenCode session locally.
- Model, mode, and permission behavior can still vary by installed OpenCode runtime.
- Tool events are normalized into Kaiwu’s canonical tool timeline.
- For OpenCode reattach flows, the supported command is `happier attach <session-id>`.

## Troubleshooting

If OpenCode is not detected:

1. Verify `opencode --version` works.
2. Restart Kaiwu.
3. Set `HAPPIER_OPENCODE_PATH` if your binary is not in PATH.

### The session is visible in the app but I want the TUI again

Use:

```bash
happier attach <session-id>
```

Do this on the same machine and against the same Kaiwu account/server profile that owns the session.

### The terminal is open but no longer accepts prompts

This usually means the session was detached back to remote mode from Kaiwu. Reattach with `happier attach <session-id>` from a terminal on the same machine.

## Related

- [Agent capabilities](/agents/capabilities) — what this agent supports, generated from the manifest.
- [Choosing an agent](/agents)
- [Model and engine selection](/agents/model-and-engine-selection)
- [Agent authentication](/agents/provider-authentication)
