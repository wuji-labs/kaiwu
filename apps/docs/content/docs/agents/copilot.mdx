---
title: GitHub Copilot
description: Configure GitHub Copilot in Kaiwu, including local CLI login, auth detection, and machine-specific setup.
---

Kaiwu can run **GitHub Copilot CLI** through the local `copilot` binary on your machine.

<Callout type="info">
  Copilot is an **experimental** integration. It works and people use it
  daily, but its surface is younger than Claude, Codex or OpenCode and is
  more likely to change. What it supports today is in
  [Agent capabilities](/agents/capabilities).
</Callout>

## Requirements

- Copilot CLI installed locally
- binary available on `PATH` (or configured through Kaiwu’s CLI path settings)

## Install Copilot

Kaiwu can install it for you:

```bash
happier install provider copilot
copilot --version
```

Or install manually:

```bash
npm install -g @github/copilot
copilot --version
```

## Local authentication in Kaiwu

Kaiwu shows Copilot’s machine-local auth state in:

- **Settings → AI & Agents → AI backends → Copilot**

From there you can:

- check whether Copilot is authenticated on the selected machine
- open the native `copilot login` flow directly inside Kaiwu
- refresh the auth status with **Check now**

Kaiwu currently detects Copilot auth from:

- `COPILOT_GITHUB_TOKEN`
- `GH_TOKEN`
- `GITHUB_TOKEN`
- the local GitHub CLI auth state when available

For the shared user guide, see [Provider authentication](/agents/provider-authentication).

## Notes

- Copilot auth in Kaiwu is machine-local. If you log in on one machine, another machine still needs its own local login.
- If you prefer token-based auth, Kaiwu can detect that and show it as an environment-backed method.

## Troubleshooting

If Copilot is not detected:

1. Verify `copilot --version` works.
2. Restart Kaiwu.
3. Re-open the provider settings page and use **Check now**.

## Related

- [Agent capabilities](/agents/capabilities) — what this agent supports, generated from the manifest.
- [Choosing an agent](/agents)
- [Model and engine selection](/agents/model-and-engine-selection)
- [Agent authentication](/agents/provider-authentication)
