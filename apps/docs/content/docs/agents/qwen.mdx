---
title: Qwen
description: Configure Qwen in Kaiwu via ACP-backed local runtime integration.
---

Kaiwu supports Qwen as a local CLI-backed provider.

<Callout type="info">
  Qwen Code is an **experimental** integration. It works and people use it
  daily, but its surface is younger than Claude, Codex or OpenCode and is
  more likely to change. What it supports today is in
  [Agent capabilities](/agents/capabilities).
</Callout>

## Requirements

- Qwen CLI installed locally.
- Binary available on `PATH` (or set `HAPPIER_QWEN_PATH`).

## Install Qwen

Kaiwu can install it for you:

```bash
happier install provider qwen
qwen --version
```

Or install manually (macOS / Linux):

```bash
curl -fsSL https://qwen-code-assets.oss-cn-hangzhou.aliyuncs.com/installation/install-qwen.sh | bash
qwen --version
```

Windows (Run as Administrator in CMD):

```bat
curl -fsSL -o %TEMP%\\install-qwen.bat https://qwen-code-assets.oss-cn-hangzhou.aliyuncs.com/installation/install-qwen.bat && %TEMP%\\install-qwen.bat
qwen --version
```

## Local authentication in Kaiwu

Qwen supports launching its native auth flow from:

- **Settings → AI & Agents → AI backends → Qwen Code**

Kaiwu opens Qwen and sends `/auth` for you.

Auth-status detection for Qwen is currently conservative, so Kaiwu may still show **Unknown** even though the login flow is available.

For the shared user guide, see [Provider authentication](/agents/provider-authentication).

## Start Qwen with Kaiwu

```bash
happier qwen
```

## Notes

- Resume availability is session-specific in Kaiwu and depends on valid resume metadata plus the current backend/settings.
- Model, mode, and permission behavior can still vary by installed Qwen runtime.
- Tool calls are normalized to canonical Kaiwu tool renderers.

## Troubleshooting

If Qwen is not detected:

1. Verify `qwen --version` works.
2. Restart Kaiwu.
3. Set `HAPPIER_QWEN_PATH` if needed.

## Related

- [Agent capabilities](/agents/capabilities) — what this agent supports, generated from the manifest.
- [Choosing an agent](/agents)
- [Model and engine selection](/agents/model-and-engine-selection)
- [Agent authentication](/agents/provider-authentication)
