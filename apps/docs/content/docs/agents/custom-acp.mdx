---
title: Custom ACP
description: Add your own ACP-compatible CLIs to Kaiwu by defining selectable custom ACP backends.
---

**Custom ACP** is Kaiwu's generic provider family for any CLI that speaks the Agent Client Protocol (ACP).

Use it when Kaiwu does not ship a dedicated built-in provider for the tool you want to run, or when you want to add your own backend choices on top of an ACP-compatible CLI.

<Callout type="info">
  Custom ACP is an **experimental** integration. It works and people use it
  daily, but its surface is younger than Claude, Codex or OpenCode and is
  more likely to change. What it supports today is in
  [Agent capabilities](/agents/capabilities).
</Callout>

## What Custom ACP gives you

Custom ACP lets you define **custom ACP backends**.

Each custom ACP backend is both:

- the launcher definition for an ACP-compatible CLI
- the user-facing backend choice you select throughout Kaiwu

## Where to find it

Open **Settings → AI & Agents → AI backends** and scroll past the bundled
agents. Two groups sit at the bottom of that screen:

- **Built-in ACP** — the generic ACP agents Kaiwu already ships, defined in
  the shared catalog and run through the shared ACP runtime.
- **Custom backends** — yours. Each one is a selectable ACP-compatible CLI
  definition with its own launcher, defaults and auth settings.

**Add ACP backend** creates a new one.

<Callout type="info">
  Custom ACP is not a row in the backends list — it is the section underneath
  it. Looking for it among Claude, Codex and the rest is the usual reason people
  think it is missing.
</Callout>

## What a custom ACP backend contains

A custom ACP backend stores the information Kaiwu needs to launch and reason about one backend choice.

Typical backend details include:

- id / name / title
- description
- command
- arguments
- environment variables
- transport profile
- auth metadata
- capability hints
- optional default mode
- optional default model

## How Custom ACP appears in Kaiwu

Configured ACP backends are first-class backend choices.

They can appear in:

- the **new session** backend picker
- **profile compatibility** and backend-specific defaults
- **permissions** defaults
- **session launcher** flows such as review, plan, and delegate
- backend-aware guidance and suggestion surfaces

This means you do not need to remember raw commands once the backend is configured.

## Models, modes, and defaults

When the underlying ACP CLI supports it, Kaiwu can probe a configured backend directly for:

- available models
- available modes
- other ACP capability hints

That lets different custom backends behave as distinct backend choices, even when they happen to launch the same CLI with different args or env.

Custom ACP models can also be saved as favorites from the new-session engine picker. Favorites are stored per configured backend, not just per provider family.

## Authentication

Custom ACP does not imply one single provider-level auth system.

Instead, auth/readiness metadata lives with each ACP backend definition. Depending on the CLI, a backend can describe:

- a login command
- status-check support
- machine-login hints
- documentation links

Some ACP CLIs support safe non-interactive status detection. Others are effectively manual-only.

See [Provider authentication](/agents/provider-authentication) for the general model.

## Custom ACP vs built-in providers

Use a built-in provider when Kaiwu already ships a dedicated integration for that tool.

Use Custom ACP when:

- you are adding a different ACP-compatible CLI
- you want your own backend choice with custom launcher/auth/defaults metadata
- you want a generic ACP integration rather than a bespoke provider page

For example:

- use [Kiro](/agents/kiro) for Kaiwu's built-in Kiro integration
- use **Custom ACP** for arbitrary user-defined ACP backends

## Common use cases

### One CLI, multiple backend choices

If you want multiple variants of the same CLI, create multiple custom ACP backends.

That is the recommended pattern when:

- the command is mostly the same
- the auth setup is the same
- the args, env, or defaults differ per role

### Team-internal ACP agent

Use Custom ACP when your team has an internal ACP-compatible CLI that Kaiwu does not know about out of the box.

### Development and testing

Use Custom ACP when you want to point Kaiwu at a local ACP stub or test harness during development.

## What Custom ACP does not do

It does not turn an arbitrary CLI into a first-class integration with bespoke UI.

- Built-in agents like Kiro, Claude, Codex and Gemini keep their own pages and behaviour.
- Custom backends run through the generic ACP runtime path.
- How much works depends on what your ACP CLI actually exposes — Kaiwu can only surface capabilities the agent advertises.

## When something looks wrong

**The backend does not appear as a choice.** Check that it still exists, has a
valid id and title, and that its command is still configured.

**Two backends running the same CLI behave differently.** Expected, if they use
different arguments or environment, probe different ACP metadata, or carry
different default mode and model values. That is the point of defining them
separately.

**You expected it to behave like a built-in agent.** Use the built-in where one
exists — [Kiro](/agents/kiro), for instance. Custom ACP is for backends
Kaiwu does not ship.

## Related

- [Model and engine selection](/agents/model-and-engine-selection)
- [Profiles](/agents/profiles)
- [Session settings](/sessions/session-settings)
- [Provider authentication](/agents/provider-authentication)
