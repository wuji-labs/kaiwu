---
title: Agent 能力
description: 各编程 Agent 在 Kaiwu 中支持与不支持的功能，根据 Agent 清单生成。
---
Kaiwu 负责驱动各类编程 Agent，而非取而代之。因此，任意特定会话的能力边界，取决于 Kaiwu 所支持的功能与该 Agent 自身 CLI 所暴露能力的交集——这也是我们通过多张表格分别对照、而非罗列单一功能列表的原因。

本页的所有表格均生成自 `packages/agents` 中的 Agent 清单，因而反映的是当前文档对应版本的实际构建状态，而非维基页面上次被更新时的陈旧记录。表格中的破折号表示该 Agent 本身不支持该项能力，并不代表 Kaiwu 尚未对其提供支持。

在全部 14 款 Agent 中，有 3 款被标记为稳定版（Stable）：Claude、Codex、OpenCode。其余 Agent 均为实验版（Experimental）——它们完全可用，且每天都有人使用，只是集成历史较短，后续更可能发生变动。Agent 的可用性与功能的可用性相互独立；关于后者的详细说明，请参阅[服务端特性开关](/extras/feature-flags)。

## Agent 概览

| Agent | 启动命令 | 状态 | 模型 | 托管安装 |
| --- | --- | --- | --- | --- |
| **Claude** | `happier claude` | 稳定版 | 向 Agent 查询，允许自定义 ID | 官方配方 |
| **Codex** | `happier codex` | 稳定版 | 向 Agent 查询 | 官方独立安装器 |
| **OpenCode** | `happier opencode` | 稳定版 | 向 Agent 查询，允许自定义 ID | `opencode-ai` |
| **Gemini** | `happier gemini` | 实验版 | 向 Agent 查询，允许自定义 ID | `@google/gemini-cli` |
| **Auggie** | `happier auggie` | 实验版 | 向 Agent 查询 | `@augmentcode/auggie` |
| **Qwen Code** | `happier qwen` | 实验版 | 固定列表 | `@qwen-code/qwen-code` |
| **Kimi** | `happier kimi` | 实验版 | 向 Agent 查询 | 官方配方 |
| **Kilo** | `happier kilo` | 实验版 | 向 Agent 查询，允许自定义 ID | `@kilocode/cli` |
| **Kiro** | `happier kiro` | 实验版 | 固定列表，允许自定义 ID | 官方独立安装器 |
| **Custom ACP** | `happier customAcp` | 实验版 | 向 Agent 查询，允许自定义 ID | 官方配方 |
| **Pi** | `happier pi` | 实验版 | 向 Agent 查询，允许自定义 ID | `@earendil-works/pi-coding-agent` |
| **Copilot** | `happier copilot` | 实验版 | 向 Agent 查询 | `@github/copilot` |
| **Cursor** | `happier cursor` | 实验版 | 向 Agent 查询 | 官方配方 |
| **Grok** | `happier grok` | 实验版 | 向 Agent 查询 | 官方配方 |

若“托管安装”一列列出了具体软件包，Kaiwu 即可在设备详情页中直接为你安装该 Agent。若标注为官方独立安装器，则需先自行运行该安装器——确切命令请参见各 Agent 的专属页面。

**Custom ACP** 并非内置绑定的 Agent，而是用于让 Kaiwu 连接任何支持 Agent Client Protocol 的 Agent。因此该行描述的是适配器本身的能力，而非某个具体厂商的实现。详情请参见 [Custom ACP](/agents/custom-acp)。

多个子命令支持别名，你习惯的键入名称通常都能直接生效：`codex` 同样支持 `codex-acp`、`codex-mcp`、`openai`、`gpt`；`opencode` 同样支持 `open-code`；`qwen` 同样支持 `qwen-code`；`kimi` 同样支持 `kimi-cli`；`kilo` 同样支持 `kilocode`；`kiro` 同样支持 `kiro-cli`；`customAcp` 同样支持 `custom-acp`；`pi` 同样支持 `pi-coding-agent`；`copilot` 同样支持 `github-copilot`、`copilot-cli`；`cursor` 同样支持 `cursor-agent`；`grok` 同样支持 `grok-build`、`grok-cli`。

## 会话

恢复、浏览、分叉与回滚，是大家通常以为处处适用的四项功能。但事实并非如此。

| Agent | 恢复自有会话 | 分叉对话 | 从消息处分叉 | 回滚 | 声明会话列表支持 |
| --- | --- | --- | --- | --- | --- |
| **Claude** | 支持 | — | — | — | 支持 |
| **Codex** | 实验性 | 支持 | — | 支持 | 支持 |
| **OpenCode** | 支持 | 支持 | 支持 | — | 支持 |
| **Gemini** | 支持 | — | — | — | — |
| **Auggie** | 支持 | — | — | — | — |
| **Qwen Code** | 支持 | — | — | — | — |
| **Kimi** | 支持 | — | — | — | — |
| **Kilo** | 支持 | — | — | — | — |
| **Kiro** | 实验性 | — | — | — | — |
| **Custom ACP** | — | — | — | — | — |
| **Pi** | 支持 | — | — | — | — |
| **Copilot** | 支持 | — | — | — | — |
| **Cursor** | 实验性 | — | — | — | — |
| **Grok** | 实验性 | — | — | — | — |

“恢复自有会话”是指 Kaiwu 能够重新挂载该 Agent 在 Kaiwu 外部启动的对话。“分叉”则指 Agent 自身的运行时支持派生分支对话；如果 Agent 原生不支持，Kaiwu 的重放分叉依然可用，因为那是 Kaiwu 自身而非 Agent 提供的机制。详见[会话分叉](/sessions/session-forking)。

最后一列只是一项能力声明，并非功能拦截开关——目前并无代码读取该字段，因此它并不决定你是否能浏览 Agent 的自有会话。关于实际能够浏览并导入的内容，请参阅[继续会话](/sessions/continuing-a-session)。

## 执行轮次

| Agent | 引导进行中的轮次 | 会话模式 | 独立计划控件 | 采纳编辑 |
| --- | --- | --- | --- | --- |
| **Claude** | 支持 | Agent 模式（内置） | 支持 | 支持 |
| **Codex** | — | 策略预设（来自 Agent） | — | — |
| **OpenCode** | — | Agent 模式（来自 Agent） | 支持 | — |
| **Gemini** | — | — | — | — |
| **Auggie** | — | — | — | — |
| **Qwen Code** | — | — | — | — |
| **Kimi** | — | — | — | — |
| **Kilo** | — | Agent 模式（来自 Agent） | 支持 | — |
| **Kiro** | — | Agent 模式（来自 Agent） | 支持 | — |
| **Custom ACP** | — | Agent 模式（来自 Agent） | 支持 | — |
| **Pi** | 支持 | — | — | — |
| **Copilot** | — | Agent 模式（来自 Agent） | 支持 | — |
| **Cursor** | — | Agent 模式（来自 Agent） | 支持 | — |
| **Grok** | 支持 | — | — | — |

“引导”允许你在某个轮次正在执行时追加纠偏指令，而无需直接中断它。若 Agent 不支持实时引导，Kaiwu 会中断当前执行并重新发送；这种方式虽然稍慢，但丢失的上下文远比听上去要少——详见[引导](/sessions/steering)。

**会话模式不等同于权限模式。** 当模式列表*来自 Agent* 时，Kaiwu 会展示该构建版本为该会话声明的所有模式；因此不同版本间的选项可能有所差异，且当运行时未提供任何模式时，“模式”控件会自动隐藏。“独立计划控件”下方的破折号表示 Kaiwu 没有为该 Agent 提供专门的计划交互控件——而**不是**说该 Agent 缺少计划模式。例如，Codex 就是通过“模式”控件来切换到 `plan` 的。

## 媒体与工具

| Agent | 发布生成的媒体 | 声明图片输入 | 声明原生图片生成 | Kaiwu 工具 | 已连接服务 |
| --- | --- | --- | --- | --- | --- |
| **Claude** | 支持 | 支持 | — | 支持（`native_mcp`） | `claude-subscription`, `anthropic` |
| **Codex** | 支持 | 支持 | 支持 | 支持（`native_mcp`） | `openai-codex`, `openai` |
| **OpenCode** | 支持 | 实验性 | — | 支持（`native_mcp`） | `openai-codex`, `openai`, `claude-subscription`, `anthropic` |
| **Gemini** | — | 支持 | — | 支持（`native_mcp`） | `gemini` |
| **Auggie** | 实验性 | 实验性 | — | — | — |
| **Qwen Code** | 支持 | 实验性 | — | — | — |
| **Kimi** | 支持 | 实验性 | — | — | — |
| **Kilo** | 支持 | 实验性 | — | — | — |
| **Kiro** | 支持 | 实验性 | — | 支持（`native_mcp`） | — |
| **Custom ACP** | 支持 | 实验性 | — | 支持（`native_mcp`） | — |
| **Pi** | 实验性 | 实验性 | — | — | `openai-codex`, `openai`, `claude-subscription`, `anthropic` |
| **Copilot** | 支持 | 实验性 | — | — | — |
| **Cursor** | 支持 | — | 支持 | — | — |
| **Grok** | — | — | — | — | — |

其中的两列描述的是对应集成所**声明**的能力，而非 Kaiwu 强制约束的行为。当你决定是否要附加屏幕截图时，这种区别至关重要。

**发布生成的媒体**属于实际行为特性：Kaiwu 在接入 Agent 运行时会读取该项配置，因此此处标注为支持的 Agent 确实能将生成的图像输出到会话中。

**声明图片输入**与**声明原生图片生成**是对应集成对该 Agent 做出的声明，目前没有任何代码路径以此作为硬性拦截条件。在实践中，基于 ACP 的会话会直接将受信任的本地图像附加至提示词中，而不论清单中如何声明——至于 Agent 随后能否妥善处理这些图像，取决于 Agent 本身，而非 Kaiwu。因此，这两列中的破折号应理解为“未作声明”，而非“会被拒绝”，并且部分声明可能会滞后于运行时的实际能力。

## 身份验证

| Agent | 凭据存储位置 | 后台鉴权检查 |
| --- | --- | --- |
| **Claude** | `~/.claude/.credentials.json` 或 `ANTHROPIC_API_KEY`、`ANTHROPIC_AUTH_TOKEN` | 自动 |
| **Codex** | `~/.codex/auth.json` 或 `OPENAI_API_KEY`、`CODEX_API_KEY` | 自动 |
| **OpenCode** | Agent 自行管理 | 自动 |
| **Gemini** | `~/.gemini/oauth_creds.json` 或 `GEMINI_API_KEY`、`GOOGLE_API_KEY` | 自动 |
| **Auggie** | Agent 自行管理 | 自动 |
| **Qwen Code** | Agent 自行管理 | 自动 |
| **Kimi** | Agent 自行管理 | 自动 |
| **Kilo** | Agent 自行管理 | 自动 |
| **Kiro** | Agent 自行管理 | 仅手动 |
| **Custom ACP** | Agent 自行管理 | 仅手动 |
| **Pi** | `OPENAI_API_KEY`、`ANTHROPIC_API_KEY` | 自动 |
| **Copilot** | `COPILOT_GITHUB_TOKEN`、`GH_TOKEN`、`GITHUB_TOKEN` | 自动 |
| **Cursor** | `CURSOR_API_KEY` | 自动 |
| **Grok** | `XAI_API_KEY` | 自动 |

后台检查标注为“仅手动”的 Agent 不会定时重新探测；它们的状态会在你打开对应后端的设置界面或启动会话时刷新。详见[Agent 身份验证](/agents/provider-authentication)。

## 相关内容

- [编程 Agent](/agents) —— 每款 Agent 的独立页面，包含配置方法与已知限制。
- [模型与引擎选择](/agents/model-and-engine-selection)
- [服务端特性开关](/extras/feature-flags)