name: CI — Stress Tests

on:
  workflow_dispatch:
    inputs:
      repeat:
        description: "Repetitions (KAIWU_E2E_REPEAT)"
        required: true
        default: "10"
        type: string
      seed:
        description: "Seed (KAIWU_E2E_SEED, empty = random)"
        required: true
        default: ""
        type: string
  schedule:
    # Nightly (UTC)
    - cron: "30 4 * * *"

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  stress-scheduled:
    if: ${{ github.event_name == 'schedule' }}
    permissions:
      contents: read
      pull-requests: read
    uses: ./.github/workflows/tests.yml
    with:
      select_jobs_explicitly: true
      run_ui: false
      run_server: false
      run_server_db_contract: false
      run_cli: false
      run_stack: false
      run_typecheck: false
      run_cli_daemon_e2e: false
      run_e2e_core: false
      run_installers_smoke: false
      run_binary_smoke: false
      run_providers: false
      providers_preset: all
      providers_tier: smoke
      run_stress: true
      stress_config: '{"repeat":"10","seed":""}'

  stress-dispatch:
    if: ${{ github.event_name == 'workflow_dispatch' }}
    permissions:
      contents: read
      pull-requests: read
    uses: ./.github/workflows/tests.yml
    with:
      select_jobs_explicitly: true
      run_ui: false
      run_server: false
      run_server_db_contract: false
      run_cli: false
      run_stack: false
      run_typecheck: false
      run_cli_daemon_e2e: false
      run_e2e_core: false
      run_installers_smoke: false
      run_binary_smoke: false
      run_providers: false
      providers_preset: all
      providers_tier: smoke
      run_stress: true
      stress_config: ${{ format('{{"repeat":{0},"seed":{1}}}', toJSON(github.event.inputs.repeat), toJSON(github.event.inputs.seed)) }}
