name: CI — Extended DB Matrix

on:
  workflow_dispatch:
  workflow_call:
    inputs:
      checkout_sha:
        required: false
        default: ""
        type: string
      select_jobs_explicitly:
        description: "Honor run_* inputs instead of running the complete direct-dispatch matrix"
        required: false
        default: false
        type: boolean
      run_e2e_postgres:
        required: false
        default: true
        type: boolean
      run_e2e_mysql:
        required: false
        default: true
        type: boolean
      run_db_contract_postgres:
        required: false
        default: true
        type: boolean
      run_db_contract_mysql:
        required: false
        default: true
        type: boolean
  schedule:
    # Nightly (UTC)
    - cron: "0 4 * * *"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  # Keep the active failure collector intact; a newer scheduled/manual request may wait.
  cancel-in-progress: false

jobs:
  e2e-postgres:
    if: ${{ !inputs.select_jobs_explicitly || inputs.run_e2e_postgres }}
    name: Core E2E (Postgres)
    runs-on: ubuntu-22.04
    timeout-minutes: 120
    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_DB: happier
          POSTGRES_USER: happier
          POSTGRES_PASSWORD: happier
        ports:
          - 5432/tcp
        options: >-
          --health-cmd="pg_isready -U happier -d happier"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=10
    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          ref: ${{ inputs.checkout_sha || github.sha }}

      - name: Setup Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version: 22.x
          cache: yarn
          cache-dependency-path: yarn.lock

      - name: Enable Corepack
        uses: ./.github/actions/enable-corepack-yarn

      - name: Install system dependencies
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends tmux
          tmux -V

      - name: Install Sapling
        run: |
          bash scripts/ci/install_sapling_ubuntu22.sh

      - name: Verify Sapling CLI
        run: sl --version

      - name: Install dependencies
        env:
          YARN_PRODUCTION: "false"
          npm_config_production: "false"
        uses: ./.github/actions/install-yarn-dependencies
        with:
          args: --frozen-lockfile --ignore-engines

      - name: Generate server provider client (postgres)
        env:
          KAIWU_BUILD_DB_PROVIDERS: postgres
          DATABASE_URL: postgresql://happier:happier@127.0.0.1:${{ job.services.postgres.ports[5432] }}/happier?sslmode=disable
        run: yarn -s workspace @happier-dev/server generate:providers

      - name: Run core e2e database suite (postgres)
        env:
          CI: "1"
          KAIWU_E2E_SAVE_ARTIFACTS: "0"
          KAIWU_E2E_DB_PROVIDER: postgres
          KAIWU_E2E_PROVIDER_SKIP_SERVER_GENERATE: "1"
          DATABASE_URL: postgresql://happier:happier@127.0.0.1:${{ job.services.postgres.ports[5432] }}/happier?sslmode=disable
        run: yarn test:e2e:core:fast -- --no-file-parallelism --maxWorkers=1 --minWorkers=1

      - name: Upload e2e diagnostics (on failure)
        if: failure()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: extended-e2e-postgres-diagnostics
          retention-days: 7
          if-no-files-found: ignore
          path: |
            .project/logs/e2e/**/*.log
            .project/logs/e2e/**/*.json
            .project/logs/e2e/**/*.jsonl
            .project/logs/e2e/**/*.txt

  e2e-mysql:
    if: ${{ !inputs.select_jobs_explicitly || inputs.run_e2e_mysql }}
    name: Core E2E (MySQL)
    runs-on: ubuntu-22.04
    timeout-minutes: 120
    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: happier
          MYSQL_DATABASE: happier
        ports:
          - 3306/tcp
        options: >-
          --health-cmd="mysqladmin ping -h 127.0.0.1 -u root -p${MYSQL_ROOT_PASSWORD}"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=15
    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          ref: ${{ inputs.checkout_sha || github.sha }}

      - name: Setup Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version: 22.x
          cache: yarn
          cache-dependency-path: yarn.lock

      - name: Enable Corepack
        uses: ./.github/actions/enable-corepack-yarn

      - name: Install system dependencies
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends tmux
          tmux -V

      - name: Install Sapling
        run: |
          bash scripts/ci/install_sapling_ubuntu22.sh

      - name: Verify Sapling CLI
        run: sl --version

      - name: Install dependencies
        env:
          YARN_PRODUCTION: "false"
          npm_config_production: "false"
        uses: ./.github/actions/install-yarn-dependencies
        with:
          args: --frozen-lockfile --ignore-engines

      - name: Generate server provider client (mysql)
        env:
          KAIWU_BUILD_DB_PROVIDERS: mysql
          DATABASE_URL: mysql://root:happier@127.0.0.1:${{ job.services.mysql.ports[3306] }}/happier
        run: yarn -s workspace @happier-dev/server generate:providers

      - name: Run core e2e database suite (mysql)
        env:
          CI: "1"
          KAIWU_E2E_SAVE_ARTIFACTS: "0"
          KAIWU_E2E_DB_PROVIDER: mysql
          KAIWU_E2E_PROVIDER_SKIP_SERVER_GENERATE: "1"
          DATABASE_URL: mysql://root:happier@127.0.0.1:${{ job.services.mysql.ports[3306] }}/happier
        run: yarn test:e2e:core:fast -- --no-file-parallelism --maxWorkers=1 --minWorkers=1

      - name: Upload e2e diagnostics (on failure)
        if: failure()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: extended-e2e-mysql-diagnostics
          retention-days: 7
          if-no-files-found: ignore
          path: |
            .project/logs/e2e/**/*.log
            .project/logs/e2e/**/*.json
            .project/logs/e2e/**/*.jsonl
            .project/logs/e2e/**/*.txt

  db-contract-postgres:
    if: ${{ !inputs.select_jobs_explicitly || inputs.run_db_contract_postgres }}
    name: DB Contract (Postgres)
    runs-on: ubuntu-latest
    timeout-minutes: 120
    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_DB: happier
          POSTGRES_USER: happier
          POSTGRES_PASSWORD: happier
        ports:
          - 5432/tcp
        options: >-
          --health-cmd="pg_isready -U happier -d happier"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=10
    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          ref: ${{ inputs.checkout_sha || github.sha }}

      - name: Setup Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version: 22.x
          cache: yarn
          cache-dependency-path: yarn.lock

      - name: Enable Corepack
        uses: ./.github/actions/enable-corepack-yarn

      - name: Install dependencies
        env:
          YARN_PRODUCTION: "false"
          npm_config_production: "false"
        uses: ./.github/actions/install-yarn-dependencies
        with:
          args: --frozen-lockfile --ignore-engines

      - name: Migrate (Postgres)
        env:
          DATABASE_URL: postgresql://happier:happier@127.0.0.1:${{ job.services.postgres.ports[5432] }}/happier?sslmode=disable
        run: yarn --cwd apps/server -s prisma migrate deploy

      - name: Run db contract suite (Postgres)
        env:
          KAIWU_DB_PROVIDER: postgres
          DATABASE_URL: postgresql://happier:happier@127.0.0.1:${{ job.services.postgres.ports[5432] }}/happier?sslmode=disable
        run: yarn --cwd apps/server test:db-contract

  db-contract-mysql:
    if: ${{ !inputs.select_jobs_explicitly || inputs.run_db_contract_mysql }}
    name: DB Contract (MySQL)
    runs-on: ubuntu-latest
    timeout-minutes: 120
    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: happier
          MYSQL_DATABASE: happier
        ports:
          - 3306/tcp
        options: >-
          --health-cmd="mysqladmin ping -h 127.0.0.1 -u root -p${MYSQL_ROOT_PASSWORD}"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=15
    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          ref: ${{ inputs.checkout_sha || github.sha }}

      - name: Setup Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version: 22.x
          cache: yarn
          cache-dependency-path: yarn.lock

      - name: Enable Corepack
        uses: ./.github/actions/enable-corepack-yarn

      - name: Install dependencies
        env:
          YARN_PRODUCTION: "false"
          npm_config_production: "false"
        uses: ./.github/actions/install-yarn-dependencies
        with:
          args: --frozen-lockfile --ignore-engines

      - name: Migrate (MySQL)
        env:
          DATABASE_URL: mysql://root:happier@127.0.0.1:${{ job.services.mysql.ports[3306] }}/happier
        run: yarn --cwd apps/server -s migrate:mysql:deploy

      - name: Run db contract suite (MySQL)
        env:
          KAIWU_DB_PROVIDER: mysql
          DATABASE_URL: mysql://root:happier@127.0.0.1:${{ job.services.mysql.ports[3306] }}/happier
        run: yarn --cwd apps/server test:db-contract
